#!/bin/bash
MUTE=`cat ~/.mute`
test "$MUTE" = "1"
RETURN=$?
if [[ $RETURN -eq 0 ]]
then
    pactl set-sink-mute 0 0
    echo "0" > ~/.mute
    exit
fi
if [[ $RETURN -eq 1 ]]
then
    pactl set-sink-mute 0 1
    echo "1" > ~/.mute
    exit
fi
